// create a variable called sum that will store and return the answer of the solution
// take param1 and param2 and add them together and assign that to the sum variable
// return the sum variable

function solution(param1, param2) {
var sum = param1 + param2
return sum
}
